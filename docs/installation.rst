============
Installation
============

At the command line::

    $ easy_install django-dj-plugin

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-dj-plugin
    $ pip install django-dj-plugin
